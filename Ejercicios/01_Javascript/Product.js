export default class Products {
	constructor(title, price, thumbnail, code, stock) {
		this.title = title;
		this.price = price;
		this.thumbnail = thumbnail;
		this.code = code;
		this.stock = stock;
	}
}
